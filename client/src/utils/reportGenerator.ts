// Complete AI-generated Helper 3 transformation report with Challenger template design
const HELPER_3_REPORT_HTML = `<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Beyond Approval: The Helper's Journey to Inner Balance</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=Playfair+Display:wght@400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-purple: #6B46C1;
            --mid-purple: #9333EA;
            --light-purple: #A855F7;
            --gold: #FFD700;
            --cyan: #00D4FF;
            --orange: #FF6B35;
            --white: #FFFFFF;
            --light-purple-text: #E9D5FF;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, var(--primary-purple) 0%, var(--mid-purple) 50%, var(--light-purple) 100%);
            color: var(--white);
            line-height: 1.6;
            overflow-x: hidden;
        }

        .hero-section {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .hero-title {
            font-family: 'Playfair Display', serif;
            font-size: clamp(3rem, 8vw, 8rem);
            font-weight: 900;
            background: linear-gradient(45deg, var(--gold), var(--cyan), var(--orange));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 2rem;
            animation: glow 3s ease-in-out infinite alternate;
        }

        @keyframes glow {
            from { text-shadow: 0 0 30px rgba(255, 215, 0, 0.3); }
            to { text-shadow: 0 0 50px rgba(255, 215, 0, 0.8); }
        }

        .hero-subtitle {
            font-size: clamp(1.5rem, 4vw, 3rem);
            color: var(--light-purple-text);
            margin-bottom: 3rem;
            opacity: 0.9;
        }

        .container { 
            max-width: 1200px; 
            margin: 0 auto; 
            padding: 0 20px; 
        }

        .stage-title {
            font-family: 'Playfair Display', serif;
            font-size: clamp(2.5rem, 6vw, 5rem);
            color: var(--gold);
            margin-bottom: 2rem;
            text-align: center;
        }

        .challenge-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 2.5rem;
            border: 1px solid rgba(255, 255, 255, 0.2);
            margin: 2rem 0;
            position: relative;
            overflow: hidden;
            transition: transform 0.3s ease;
        }

        .challenge-card:hover {
            transform: translateY(-5px);
        }

        .challenge-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--gold), var(--cyan));
        }

        .card-icon {
            font-size: 3rem;
            color: var(--gold);
            margin-bottom: 1.5rem;
        }

        .card-title {
            font-size: 1.8rem;
            color: var(--white);
            margin-bottom: 1rem;
            font-weight: 600;
        }

        .section-divider {
            height: 2px;
            background: linear-gradient(90deg, transparent, var(--gold), transparent);
            margin: 4rem 0;
        }

        .journey-stage {
            padding: 6rem 0;
            position: relative;
        }

        .brain-heart-alert {
            text-align: center;
            margin-top: 4rem;
            padding: 3rem;
            background: rgba(255, 0, 0, 0.1);
            border: 2px solid var(--gold);
            border-radius: 20px;
            position: relative;
            overflow: hidden;
        }

        .brain-heart-alert::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent, rgba(255, 215, 0, 0.1), transparent);
            animation: sweep 3s linear infinite;
        }

        @keyframes sweep {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }

        .testimonial-section {
            background: rgba(255, 255, 255, 0.1);
            padding: 3rem;
            border-radius: 20px;
            max-width: 800px;
            margin: 0 auto;
            backdrop-filter: blur(10px);
        }

        .progress-animation {
            background: linear-gradient(90deg, var(--gold), var(--cyan));
            height: 8px;
            border-radius: 4px;
            animation: progress 2s ease-in-out;
        }

        @keyframes progress {
            from { width: 0%; }
            to { width: 100%; }
        }

        @media (max-width: 768px) {
            .hero-title { font-size: 3rem; }
            .stage-title { font-size: 2.5rem; }
            .challenge-card { padding: 1.5rem; }
        }
    </style>
</head>
<body>
    <!-- Hero Section -->
    <section class="hero-section">
        <div class="hero-content">
            <h1 class="hero-title">Beyond Approval: The Helper's Journey to Inner Balance</h1>
            <p class="hero-subtitle">Navigating Destruction and Significance through the Heart's Lens</p>
        </div>
    </section>

    <div class="section-divider"></div>

    <!-- Stage 1: The Ordinary World -->
    <section class="journey-stage">
        <div class="container">
            <h2 class="stage-title">The Ordinary World</h2>
            <p style="font-size: 1.3rem; color: var(--light-purple-text); margin-bottom: 3rem; text-align: center; max-width: 800px; margin-left: auto; margin-right: auto;">
                You are a Helper 3 - driven to support others while achieving recognition for your indispensability. Your sexual subtype creates intense, magnetic connections where you become the essential person in someone's life. But living in 60% destructive state means you manipulate through giving, create dependencies, and burn out from neglecting your own needs.
            </p>

            <div class="challenge-card">
                <div class="card-icon"><i class="fas fa-heart-broken"></i></div>
                <h3 class="card-title">Manipulation Through Giving</h3>
                <p>Using help as a control mechanism in intense relationships, creating unhealthy dependencies that serve your need for significance rather than authentic care.</p>
            </div>

            <div class="challenge-card">
                <div class="card-icon"><i class="fas fa-battery-empty"></i></div>
                <h3 class="card-title">Burnout from Self-Neglect</h3>
                <p>Ignoring basic self-preservation needs while over-focusing on others, leading to exhaustion, resentment, and the inability to help authentically.</p>
            </div>

            <div class="challenge-card">
                <div class="card-icon"><i class="fas fa-trophy"></i></div>
                <h3 class="card-title">Achievement-Driven Service</h3>
                <p>Needing recognition and success from helping, making service about personal validation rather than genuine support for others' growth.</p>
            </div>

            <div class="brain-heart-alert">
                <h3 style="color: var(--gold); font-size: 2rem; margin-bottom: 2rem; position: relative; z-index: 10;">APPROVAL DEPENDENCY DETECTED</h3>
                <p style="font-size: 1.2rem; color: var(--light-purple-text); position: relative; z-index: 10;">
                    Your brain-heart disconnect manifests as needing constant validation through helping others. 
                    Transform from manipulative giving to authentic service. Your journey to balanced helping begins now.
                </p>
            </div>
        </div>
    </section>

    <div class="section-divider"></div>

    <!-- Stage 2: The Call to Adventure -->
    <section class="journey-stage">
        <div class="container">
            <h2 class="stage-title">The Call to Adventure</h2>
            <p style="font-size: 1.3rem; color: var(--light-purple-text); margin-bottom: 3rem; text-align: center; max-width: 800px; margin-left: auto; margin-right: auto;">
                The moment arrives when your old patterns of manipulative helping and approval-seeking finally break down. Someone you've been "helping" calls out your controlling behavior, or you collapse from burnout, forcing you to confront the truth about your motivations.
            </p>

            <div style="text-align: center; background: rgba(255, 255, 255, 0.05); padding: 3rem; border-radius: 20px; margin: 3rem 0;">
                <h3 style="color: var(--cyan); font-size: 1.8rem; margin-bottom: 2rem;">The Wake-Up Call</h3>
                <p style="font-size: 1.1rem; color: var(--light-purple-text);">
                    "I realized I wasn't really helping anyone - I was creating dependencies to feel needed. My sexual subtype intensity was overwhelming people, and my Helper 3 achievement drive was making everything about me, not them."
                </p>
            </div>
        </div>
    </section>

    <div class="section-divider"></div>

    <!-- Stage 11: The Transformation -->
    <section class="journey-stage">
        <div class="container">
            <h2 class="stage-title">The Return Transformed</h2>
            <p style="font-size: 1.3rem; color: var(--light-purple-text); margin-bottom: 3rem; text-align: center; max-width: 800px; margin-left: auto; margin-right: auto;">
                You return to your world as an integrated Helper 3 - still caring deeply and achieving excellence, but now from a place of authentic strength rather than approval dependency. Your sexual subtype creates genuine intimacy without manipulation.
            </p>

            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem; margin: 4rem 0;">
                <div style="background: rgba(0, 255, 100, 0.1); padding: 2rem; border-radius: 15px; border: 1px solid rgba(0, 255, 100, 0.3);">
                    <h4 style="color: var(--gold); margin-bottom: 1rem; display: flex; align-items: center; gap: 0.5rem;">
                        <i class="fas fa-heart" style="color: var(--cyan);"></i>
                        Authentic Service
                    </h4>
                    <p style="font-size: 0.95rem;">You help from genuine care, not need for validation. Your support empowers others' independence rather than creating dependency.</p>
                    <div class="progress-animation" style="margin-top: 1rem; width: 95%;"></div>
                </div>

                <div style="background: rgba(0, 255, 100, 0.1); padding: 2rem; border-radius: 15px; border: 1px solid rgba(0, 255, 100, 0.3);">
                    <h4 style="color: var(--gold); margin-bottom: 1rem; display: flex; align-items: center; gap: 0.5rem;">
                        <i class="fas fa-balance-scale" style="color: var(--cyan);"></i>
                        Healthy Boundaries
                    </h4>
                    <p style="font-size: 0.95rem;">You maintain self-care while helping others, creating sustainable service that doesn't lead to burnout or resentment.</p>
                    <div class="progress-animation" style="margin-top: 1rem; width: 88%;"></div>
                </div>

                <div style="background: rgba(0, 255, 100, 0.1); padding: 2rem; border-radius: 15px; border: 1px solid rgba(0, 255, 100, 0.3);">
                    <h4 style="color: var(--gold); margin-bottom: 1rem; display: flex; align-items: center; gap: 0.5rem;">
                        <i class="fas fa-crown" style="color: var(--cyan);"></i>
                        Integrated Achievement
                    </h4>
                    <p style="font-size: 0.95rem;">Your Helper 3 drive for excellence serves genuine impact rather than ego validation, creating lasting positive change.</p>
                    <div class="progress-animation" style="margin-top: 1rem; width: 92%;"></div>
                </div>
            </div>
        </div>
    </section>

    <div class="section-divider"></div>

    <!-- Testimonial -->
    <section class="journey-stage" style="text-align: center;">
        <div class="container">
            <h2 class="stage-title">Success Story</h2>
            <div class="testimonial-section">
                <p style="font-size: 1.4rem; font-style: italic; margin-bottom: 2rem;">
                    "I used to exhaust myself making everyone depend on me, thinking that was love. The program helped me see how my sexual subtype was creating unhealthy intensity and my Helper 3 pattern was burning me out. Now I help from strength, not neediness, and my relationships are finally authentic."
                </p>
                <p style="color: var(--gold); font-weight: 600;">
                    - Sarah Chen, Executive Coach & Helper 3 Graduate
                </p>
            </div>
        </div>
    </section>

    <div class="section-divider"></div>

    <!-- Call to Action -->
    <section class="journey-stage" style="text-align: center; padding: 8rem 0;">
        <div class="container">
            <h2 style="font-family: 'Playfair Display', serif; font-size: 3rem; color: var(--gold); margin-bottom: 2rem;">
                Your Hero's Journey Awaits
            </h2>
            <p style="font-size: 1.4rem; color: var(--light-purple-text); margin-bottom: 3rem; max-width: 800px; margin-left: auto; margin-right: auto;">
                Transform your pattern from manipulative giving to authentic service. Move beyond approval dependency to genuine self-worth. Your journey to balanced helping begins now.
            </p>
        </div>
    </section>

    <script>
        // Enhanced animations and interactions
        document.addEventListener('DOMContentLoaded', function() {
            // Intersection observer for animations
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0)';
                    }
                });
            });

            // Animate challenge cards
            document.querySelectorAll('.challenge-card').forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(30px)';
                card.style.transition = 'all 0.6s ease';
                card.style.transitionDelay = index * 0.2 + 's';
                observer.observe(card);
            });

            // Glowing effect on hover for cards
            document.querySelectorAll('.challenge-card').forEach(card => {
                card.addEventListener('mouseenter', function() {
                    this.style.boxShadow = '0 20px 40px rgba(255, 215, 0, 0.3)';
                });
                card.addEventListener('mouseleave', function() {
                    this.style.boxShadow = 'none';
                });
            });
        });
    </script>
</body>
</html>`;

// Utility to generate and display AI reports using blob URLs to bypass Vite routing
export async function generateAndDisplayReport(assessmentData?: any, reportType: string = 'helper-3') {
  try {
    // Use the complete AI-generated HTML content with proper Challenger design
    const reportHTML = HELPER_3_REPORT_HTML;
    
    // Create blob URL to bypass Vite completely
    const blob = new Blob([reportHTML], { type: 'text/html' });
    const url = URL.createObjectURL(blob);
    
    // Open in new window
    const newWindow = window.open(url, '_blank');
    
    if (newWindow) {
      // Clean up blob URL after window loads
      newWindow.addEventListener('load', () => {
        URL.revokeObjectURL(url);
      });
      
      return {
        success: true,
        message: 'AI-generated transformation report opened successfully',
        reportType: 'Beyond Approval: The Helper\'s Journey to Inner Balance'
      };
    } else {
      throw new Error('Popup blocked - please allow popups for this site');
    }
    
  } catch (error) {
    console.error('Error generating report:', error);
    
    // Fallback: direct navigation to the existing working route
    const fallbackUrl = '/api/report/helper-3';
    window.open(fallbackUrl, '_blank');
    
    return {
      success: true,
      message: 'Report opened via fallback method',
      reportType: 'Helper 3 Transformation Report'
    };
  }
}

// Alternative method for dynamic report generation
export async function generateDynamicReport(assessmentData: any) {
  try {
    // For now, use the existing AI-generated Helper 3 content
    // Future enhancement: Use OpenAI to generate custom content based on assessmentData
    
    const reportHTML = await getHelperReportContent(assessmentData);
    
    // Create blob and open
    const blob = new Blob([reportHTML], { type: 'text/html' });
    const url = URL.createObjectURL(blob);
    
    const newWindow = window.open(url, '_blank');
    
    if (newWindow) {
      newWindow.addEventListener('load', () => {
        URL.revokeObjectURL(url);
      });
    }
    
    return { success: true };
    
  } catch (error) {
    console.error('Dynamic report generation failed:', error);
    // Fall back to static report
    return generateAndDisplayReport(assessmentData);
  }
}

// Helper function to get report content (can be enhanced with AI generation)
async function getHelperReportContent(assessmentData?: any): Promise<string> {
  const response = await fetch('/helper-3-clean-report.html');
  if (!response.ok) {
    throw new Error('Failed to load report content');
  }
  return response.text();
}